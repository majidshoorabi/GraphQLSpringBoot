# Gets Majid's Bank account
query GET_BANK_ACCOUNT($id: ID){
    bankAccount(id: $id) {
        id
        currency
        client {
            id
            firstname
            lastname
        }
    }
}